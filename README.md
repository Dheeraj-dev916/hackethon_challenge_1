# hackethon_challenge_1
Goal: Turn any unstructured text (report, article, dashboard) into a concrete, simulated action using Google Antigravity as the orchestrator. Backend: FastAPI (/analyze) runs the Antigravity workflow (Planner → Content → Insight → Impact → Decision → Simulation). 
